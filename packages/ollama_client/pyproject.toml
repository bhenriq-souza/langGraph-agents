[project]
name = "agents-ollama-client"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "agents-core",
    "httpx>=0.27",
]

[tool.uv.sources]
agents-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
