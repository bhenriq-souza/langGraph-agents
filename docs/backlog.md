# Backlog

Task format per spec 0000: `T-<spec>-<nn>` · **What** · **Where** · **Done when** (AC/INV IDs from the referenced spec, plus the global DoD of spec 0000: quality pipeline green). Tasks are listed in recommended execution order. Check a task off only when its "done when" is verified.

## Phase 0 — Foundation

- [ ] **T-0013-01** — Initialize git repo with `develop` as default branch, root `pyproject.toml` with UV workspace members, `.python-version` (3.12), `.gitignore` (incl. `reports/`), member skeletons (`packages/core`, `packages/ollama_client`, `agents/ai_repo_auditor`, `mcp_servers/ai_repo_auditor_mcp`) with `src/` layout, `py.typed`, empty test suites. · Where: repo root. · Done when: `uv sync` + `uv run pytest` green from clean clone (AC-0013-01, INV-0013-01, 03).
- [ ] **T-0013-02** — Configure ruff (format+lint, line 100, rule sets per spec), pyright (strict on packages+domain), bandit, import-linter contracts, pytest+cov config, pre-commit hooks incl. commitizen on `commit-msg` stage. · Where: root `pyproject.toml`, `.pre-commit-config.yaml`, `.importlinter`. · Done when: each gate fails on a planted canary violation, then passes clean (AC-0013-02); a non-conventional commit message is rejected locally (AC-0014-02).
- [ ] **T-0014-01** — Activate GitHub workflow: create remote repo with `develop` as default branch, push planning artifacts (bootstrap exception), enable branch protection on `develop` (1 owner approval via CODEOWNERS — handle `bhenriq-souza` already confirmed —, required `ci` check, dismiss stale approvals, no direct/force pushes). · Where: GitHub settings + `.github/`. · Done when: AC-0014-01; direct push to `develop` is rejected.
- [ ] **T-0014-02** — Validate the workflow end-to-end with a canary PR: branch ritual from `develop`, conventional commit, PR template rendered, CI runs the gates, merge blocked until owner approval, squash merge with conventional title, branch auto-deleted. · Where: n/a (process validation). · Done when: AC-0014-03, 04.
- [ ] **T-0000-01** — Write `scripts/check_specs.py` (frontmatter + sections + depends_on resolution) and `scripts/check_language.py` (forbidden-language grep over specs and, later, source strings). · Where: `scripts/`. · Done when: both pass on the current spec tree; check_specs fails on a mutilated spec fixture (AC-0000-01, INV-0000-01..03).
- [ ] **T-0012-01** — Implement `AgentsSettings` (all keys of spec 0012, env prefix, `.env`, validation incl. loopback check) + `configure_structlog` + core error types with codes from spec 0002. · Where: `packages/core/src/agents_core/{settings,logging,errors}.py`. · Done when: AC-0012 settings behaviors + AC-0010-05; INV-0012-03.
- [ ] **T-0010-01** — Implement `SecurityPolicy` (resolve-then-confine algorithm, report-target validation, `is_within_repo`). · Where: `packages/core/src/agents_core/security.py`. · Done when: AC-0010-01, 02 (traversal, symlink, prefix-cousin, relative, nonexistent cases).
- [ ] **T-0007-01** — Implement `agents_ollama_client`: httpx-based client with `health()`, `list_models()`, `chat_json()` (format=json, options, timeout), typed responses, transport seam for fakes. · Where: `packages/ollama_client/src/agents_ollama_client/`. · Done when: unit tests over a mocked transport cover success, timeout, connection error; INV-0007-04 honored via settings URL.
- [ ] **T-0011-01** — Implement `scripts/build_fixture_repo.py` (scripted history per spec 0011: AI-ish file, human-ish file, minified, generated, lockfile, node_modules, binary, escaping symlink, canary string; fixed dates) + shared `conftest.py` fixture + fake Ollama transport with programmable behaviors and call counter. · Where: `scripts/`, `tests/conftest.py`. · Done when: AC-0011-02 (deterministic hashes); fake behaviors unit-tested.

## Phase 1 — MVP pipeline

- [ ] **T-0002-01** — Define `AnalysisRequest`, `AnalysisResponse`, `TopFile`, `AnalysisMetadata` (exact fields/constraints of spec 0002). · Where: `packages/core/src/agents_core/contracts.py`. · Done when: validation tests incl. `extra="forbid"` and bounds (AC-0002-04 groundwork).
- [ ] **T-0003-01** — Define `AuditState`, `RepoContext`, `BudgetTracker`, `NodeError` and remaining internal models (`FileInventory`, `ClassifiedFile`, `GitFileSignals`, `CodeChunk`, `ChunkScore`, `FileScore`, `AggregateResult`, `ReportPaths`). · Where: `agents/ai_repo_auditor/src/ai_repo_auditor/domain/models.py` (+ core where shared). · Done when: models round-trip; field names match specs 0003–0008 verbatim.
- [ ] **T-0004-01** — Implement `FileScanner` (`discover_files`): git-based enumeration with os.walk fallback, `.gitignore` via pathspec, hard pre-scan limits. · Where: `.../infrastructure/scanner.py`. · Done when: AC-0004-02, 04; INV-0004-03 (symlink handling).
- [ ] **T-0004-02** — Implement filter rules + priority ordering as pure domain functions (rule table of spec 0004, first-match-wins, reasons, budget reclassification). · Where: `.../domain/filter_rules.py`. · Done when: AC-0004-01, 03, 05; INV-0004-02, 04.
- [ ] **T-0005-01** — Implement `GitCollector` (allowlisted subprocess, batched `log --numstat` parsing, timeouts, minimal env) producing `GitFileSignals`. · Where: `.../infrastructure/git_collector.py`. · Done when: AC-0005-01, 02, 04, 05; INV-0005-01, 02.
- [ ] **T-0005-02** — Implement `git_prior` pure function (bounded contribution table + clamp). · Where: `.../domain/git_prior.py`. · Done when: AC-0005-03; INV-0005-03.
- [ ] **T-0006-01** — Implement `LineWindowChunker` (boundary backtracking, merge rule, normalization, hashing). · Where: `.../domain/chunker.py`. · Done when: AC-0006-01..05; INV-0006-01..03 (property test for reconstruction).
- [ ] **T-0007-02** — Implement `OllamaChunkScorer`: prompt v1 rendering (golden files), `chat_json` call, Pydantic response validation, retry ladder, degradation states, cache seam (Noop). · Where: `.../infrastructure/llm_scorer.py`, prompt text in `.../infrastructure/prompts/scoring_v1.py`. · Done when: AC-0007-01..05; INV-0007-01..03.
- [ ] **T-0008-01** — Implement `ScoreAggregator` (formulas of spec 0008, exactly). · Where: `.../domain/aggregation.py`. · Done when: AC-0008-01..04 incl. worked-example fixture and hypothesis property test.
- [ ] **T-0009-01** — Implement `ReportRenderer` (Markdown template + JSON schema, standard limitations block) and `ReportStorage` (policy check, mkdir, atomic replace). · Where: `.../infrastructure/{report_renderer,report_storage}.py`. · Done when: AC-0009-01..04; INV-0009-01..05.
- [ ] **T-0012-02** — Implement `AuditorContainer` and `build_graph(...)` factory signature (services in, compiled graph out); Clock protocol + SystemClock. · Where: `.../container.py`, `.../graph/build.py`. · Done when: AC-0012-01, 02, 04; INV-0012-01, 02 (import-linter).
- [ ] **T-0003-02** — Implement all LangGraph nodes + conditional error routing + budget enforcement between scoring batches; wire into `build_graph`. · Where: `.../graph/nodes.py`. · Done when: AC-0003-01..05; INV-0003-01..05.
- [ ] **T-0002-02** — Implement MCP server: FastMCP app, tool registration with schema, boundary validations, error mapping table, correlation-id logging, stdio entrypoint (`main`) + HTTP flag (loopback bind). · Where: `mcp_servers/ai_repo_auditor_mcp/src/ai_repo_auditor_mcp/server.py`. · Done when: AC-0002-01..04; INV-0002-01..04; AC-0013-03.
- [ ] **T-0011-02** — Integration suite: `test_readonly_guarantee.py` (tree hash before/after — AC-0001-03, AC-0010-03), `test_log_hygiene.py` (AC-0010-04), full-pipeline run with fake Ollama over fixture repo. · Where: `tests/integration/`. · Done when: listed ACs pass.
- [ ] **T-0011-03** — E2E suite: MCP stdio client invoking the tool end-to-end with fake Ollama (AC-0001-01, 02; AC-0002-02) + error-path e2e (AC-0001-04, 05). · Where: `tests/e2e/test_mcp_analysis.py`. · Done when: listed ACs pass; `live_ollama`-marked smoke test written (excluded from default run).
- [ ] **T-0001-01** — MVP sign-off: run the live smoke test against a real repo with real `qwen2.5-coder:14b`; verify AC-0001-01..06 checklist; mark specs `implemented`. · Where: n/a. · Done when: spec 0001 DoD fully green.

## Phase 2 — Hardening (task stubs; refine specs before starting)

- [ ] **T-0007-03** — PostgreSQL `ChunkCache` implementation (psycopg, DSN from settings, idempotent schema, no-op degradation when unreachable) + container swap + hit-rate test (ADR 0007).
- [ ] **T-0003-03** — `review_report_with_second_model` node (spec 0003 extension; requires spec revision first per spec 0000).
- [ ] **T-0002-03** — Validate HTTP transport + document real Odysseus registration; update spec 0002 host section with verified config.
- [ ] **T-0008-02** — Calibration pass: run on ≥ 3 repos of known provenance; tune prior weights/confidence formula; record findings in a calibration note under `docs/`.
